# The CLI (events_tool.cli) is built entirely on the Python 3 standard
# library — no dependencies needed to use it.
#
# The Phase 2 web app (events_tool.web) needs Flask, which brings in
# Werkzeug (password hashing, dev server) as a transitive dependency.
flask>=3.0
